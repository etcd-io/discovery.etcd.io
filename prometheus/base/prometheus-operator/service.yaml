apiVersion: v1
kind: Service
metadata:
  labels:
    app: prometheus-operator-operator
    release: prometheus-operator
  name: prometheus-operator-operator
spec:
  ports:
  - name: http
    port: 8080
    targetPort: http
  selector:
    app: prometheus-operator-operator
    release: prometheus-operator
  type: ClusterIP
